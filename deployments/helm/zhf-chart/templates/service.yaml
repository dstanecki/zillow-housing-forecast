apiVersion: v1
kind: Service
metadata:
  name: {{ include "zhf-chart.fullname" . }}
  labels:
    {{- include "zhf-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zhf-chart.selectorLabels" . | nindent 4 }}
